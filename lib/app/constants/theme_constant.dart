import 'package:flutter/material.dart';

class ThemeConstant {
  ThemeConstant._();

  // Define your application's primary colors
  static const Color primaryColor =
      Color(0xFF108A00); // Your app's primary color
  static const Color darkPrimaryColor =
      Color(0xFF1E1E1E); // Dark mode primary color
  static const Color appBarColor =
      Color.fromARGB(0, 255, 255, 255); // App bar color
}
